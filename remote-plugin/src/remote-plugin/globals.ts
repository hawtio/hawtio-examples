import { Logger } from '@hawtio/react'

export const pluginName = 'remote-plugin'
export const pluginTitle = 'Remote Plugin'
export const pluginPath = '/remote-plugin'

export const log = Logger.get(pluginName)
